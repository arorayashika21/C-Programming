//WAP to calculate the average of 3 subjects from the user
#include <stdio.h>

int main() {
int a,b,c,avg;
  printf("\nEnter the Marks in three subjects:");
  scanf("%d %d %d",&a,&b,&c);
  avg=(a+b+c)/3;
  printf("Average Marks %d",avg);

  return 0;
}
