#include <stdio.h>
int main() {
int i, num, count = 0;

for (i = 1; i <= 10; i++) 
{printf("Enter number %d: ", i);
scanf("%d", &num);

if (num % 2 == 0) 
{count = count + 1;} 
}

printf("Total even numbers = %d\n", count);
return 0;
}

