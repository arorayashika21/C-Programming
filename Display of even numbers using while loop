#include <stdio.h>
int main() {
int i, n;
  printf("Enter the maximum number up to which even numbers should be printed: ");
  scanf("%d",&n);
  i=2;
  while(i<=n)
  {
  printf("\n %d",i);
  i+=2;
  }
    return 0;
}
